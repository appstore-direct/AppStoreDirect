@_exported import DeviceKit
@_exported import StoreBridge

/// Umbrella module. The app target imports only this.
public enum AppStoreDirect {
    public static let bundleIdentifier = "com.appstoredirect.mac"
}
